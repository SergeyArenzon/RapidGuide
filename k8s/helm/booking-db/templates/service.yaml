apiVersion: v1
kind: Service
metadata:
  name: {{ template "booking-db.fullname" . }}
  labels:
    {{- include "booking-db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: postgres
      protocol: TCP
      name: postgres
  selector:
    {{- include "booking-db.selectorLabels" . | nindent 4 }} 