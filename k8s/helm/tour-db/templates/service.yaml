apiVersion: v1
kind: Service
metadata:
  name: {{ template "tour-db.fullname" . }}
  labels:
    {{- include "tour-db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: postgres
      protocol: TCP
      name: postgres
  selector:
    {{- include "tour-db.selectorLabels" . | nindent 4 }} 