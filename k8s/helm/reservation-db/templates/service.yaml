apiVersion: v1
kind: Service
metadata:
  name: {{ template "reservation-db.fullname" . }}
  labels:
    {{- include "reservation-db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: postgres
      protocol: TCP
      name: postgres
  selector:
    {{- include "reservation-db.selectorLabels" . | nindent 4 }}
